@model Cuahanggiayfinal.Models.CATEGORY

@{
    ViewBag.Title = "XoaCategory";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
<div class="content">
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <h4 class="card-title">Bạn có muốn xóa Cat này không?</h4>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <h4>Category</h4>
                        <hr />
                        <dl class="dl-horizontal">


                            <dd>
                                @Html.DisplayFor(model => model.catName)
                            </dd>

                        </dl>

                        @using (Html.BeginForm())
                        {
                            @Html.AntiForgeryToken()

                            <div class="form-actions no-color">
                                <input type="submit" value="Delete" class="btn btn-default" /> |
                                @Html.ActionLink("Trở về", "GetCat")
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>

</div>

